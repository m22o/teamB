<%= form_for(@car) do |f| %>
  <% if @car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :car_number %><br>
    <%= f.text_field :car_number ,class: 'form-control'%>

    <%= f.label :color %><br>
    <%= f.text_field :color, class: 'form-control'%>
  </div>

    <%= f.submit class: "btn btn-primary"%>
<% end %>
